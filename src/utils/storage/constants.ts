export enum SecureStorageKey {
  SecureCode = 'secureCode',
}

export enum DefaultStorageKey {
  Locale = 'locale',
  MobileAppBannerIsClosed = 'mobileAppBannerIsClosed',
}

export const KEY_PREFIX = 'lou_'
